version: 2
jobs:
  build:
    docker: # See https://docs.docker.com/get-started/#docker-concepts if you are new to Docker.
      - image: docker
    steps:
      - checkout
      - run:
        name: build image 
        command: docker build -t ddns-test ./dockerfile